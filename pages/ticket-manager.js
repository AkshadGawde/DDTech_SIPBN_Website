// pages/ticket-dashboard.js

import withAuth from '../lib/withAuth';


const TicketManager = () => {
    return (
        <div>
            Will add after user side
        </div>
    );
};

export default withAuth(TicketManager);
