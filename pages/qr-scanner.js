// pages/qr-scanner.js

import withAuth from '../lib/withAuth';


const QRScanner = () => {
    return (
        <div>
            Will add after user side
        </div>
    );
};

export default withAuth(QRScanner);
